<%= form_for(@cathegory) do |f| %>
  <% if @cathegory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cathegory.errors.count, "error") %> prohibited this cathegory from being saved:</h2>

      <ul>
      <% @cathegory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
