<%= form_for(@user_act) do |f| %>
  <% if @user_act.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_act.errors.count, "error") %> prohibited this user_act from being saved:</h2>

      <ul>
      <% @user_act.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_activity %><br>
    <%= f.number_field :id_activity %>
  </div>
  <div class="field">
    <%= f.label :id_user %><br>
    <%= f.number_field :id_user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
