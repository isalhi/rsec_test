

<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_creator, hidden: true %><br>
    <%= f.number_field :id_creator, hidden: true %>
  </div>
  <div class="field">
    <%= f.label :id_activity, hidden: true %><br>
    <%= f.number_field :id_activity, hidden: true %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message, rows: 15, cols: 80 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
